{
  "name": "wfgg-api-staging-build",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "scripts": {
    "stage": "node scripts/stage.mjs && node scripts/expose-safe-manager-errors.mjs",
    "check": "node --check src/index.js && node --check src/lastwar-identity.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
