name: Portal API Worker deploy helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  permissions-note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Manual helper only.'
          echo 'Current Cloudflare token can deploy Pages but does not have Workers Scripts Edit.'
          echo 'Sentinel production uses the OWNER-only Pages edge runner and does not depend on this deployment.'
