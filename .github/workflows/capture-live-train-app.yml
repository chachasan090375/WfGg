name: Train interaction verification (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Train interactions v8 verified successfully.'
