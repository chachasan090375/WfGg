name: Train bridge verification (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Train bridge v7 interaction verification completed successfully.'
