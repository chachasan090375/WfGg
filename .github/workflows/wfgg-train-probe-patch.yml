name: WfGg Train dangling token fix (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary patch workflow disabled; dangling token fix is deployed.'
