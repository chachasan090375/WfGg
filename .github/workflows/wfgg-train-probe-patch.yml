name: WfGg Train session probe patch (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary patch workflow disabled; application code already updated.'
