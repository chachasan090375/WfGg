WfGg Radar V4 rebuild trigger.

Player-base map tiles are WorldPointInfo f2=6; nested player details are in f3.
The V3 helper patch now corrects the provisional f10 mapping to f3 before the V4 build.
