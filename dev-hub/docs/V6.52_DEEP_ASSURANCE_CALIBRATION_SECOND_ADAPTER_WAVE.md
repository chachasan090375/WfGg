# V6.52 — Deep Assurance Calibration & Second Benchmark Adapter Wave

Status: IMPLEMENTATION / NOT ACQUIRED

V6.52 deepens the first executable benchmark wave and adds five high-risk platform agents.

## First-wave deep calibration

Guardian, Sentinel, Bastion and Autonomous Recovery are now executed twice per benchmark cycle.

Independent oracle outputs are compared for:
- repeatable case verdicts;
- repeatable dimension scores;
- execution duration guardrails.

This promotes two additional benchmark-only dimensions:
- drift_resistance;
- efficiency.

The benchmark remains fixture evidence, never production truth.

## Second executable adapter wave

New executable adapters:
- Security Reviewer — real architecture-specialist contract runtime, security-scoped, model call disabled;
- Recovery Engineer — real Recovery Drill sandbox;
- Platform/Cloud Engineer — real platform-selftest adapter with temporary revision proof;
- Data Architect — real architecture-specialist contract runtime, data-scoped, model call disabled;
- Release Engineer — real external dual-assurance release gate.

No adapter requires external network access, real credentials, destructive production actions or paid providers.

## Stronger evidence promotion

The benchmark promoter now requires the exact oracle identifier declared in policy.
Promoted evidence carries promoted_at.
Fleet Observatory explicitly selects the freshest promoted benchmark revision for each agent.

Production/runtime measurements still override all benchmark measurements.

## Expected evidence effect

The four V6.51 priority agents move from at least 40% to at least 60% measured coverage.
The five second-wave agents obtain at least six benchmark-only measured dimensions and leave MEASURE_FIRST.

All evolution authority remains outside the benchmark:
Agent Foundry owns agent candidates.
Technology Watch freshness is mandatory.
Logician falsification remains mandatory.
Guardian/Sentinel remain mandatory.
Architecture Council remains final authority.
Automatic external spend remains 0 EUR.
